# 인수인계 문서

## 작업 전 확인 내용
- 저장소 초기 상태는 사실상 빈 상태(`.gitkeep`만 존재)였습니다.
- 별도 기존 운영 문서/코드가 없어 신규 스캐폴딩으로 구성했습니다.

## 이번 작업 내용
- 3개 사이트 대상 크롤러 구현:
  - `scrapers/opgg.py` (requests + BeautifulSoup)
  - `scrapers/lolps.py` (Playwright 렌더링 + BS4 파싱)
  - `scrapers/deeplol.py` (Playwright 렌더링 + BS4 파싱)
- 공통 유틸 추가: `scrapers/common.py`
  - 퍼센트/티어/포지션 정규화
  - 패치/업데이트 텍스트 추출
  - 타깃 URL allowlist(간단 SSRF 방어)
  - retry/timeout/polite delay 지원
- CLI 엔트리포인트 추가: `main.py`
  - `--site all|opgg|lolps|deeplol`
  - 결과 저장: 기본 `output/champion_stats.json`
- 디버그 아티팩트 저장 경로 반영:
  - `debug/raw_html/lolps.html`, `debug/raw_html/deeplol.html`
  - `debug/raw_text/lolps.txt`, `debug/raw_text/deeplol.txt`
- 문서화: `README.md`, 의존성: `requirements.txt`
- 간단 단위 테스트 추가: `tests/test_common.py`

## 후속 작업 권장
- 실서비스 네트워크에서 실제 셀렉터 검증 및 조정.
- 필요 시 사이트별 더 정밀한 CSS selector 버전 고정.
- CI에 playwright browser install/테스트 파이프라인 추가.
